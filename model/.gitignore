# Python virtualenv (recreate with: python3 -m venv venv && pip install -r requirements.txt)
venv/

# Python bytecode caches
__pycache__/
*.pyc

# Local tooling
.claude/

# macOS cruft
.DS_Store

# Local audio samples / captured call recordings (may contain caller voice)
*.wav
